import { LightbulbIcon as LucideProps, Github, Chrome as Google, User, type LucideIcon } from 'lucide-react'

export type Icon = LucideIcon

export const Icons = {
  gitHub: Github,
  google: Google,
  user: User,
}
